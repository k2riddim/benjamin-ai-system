# Environment configuration for Qdrant vector store script
# The script automatically uses the same environment variables as your agentic app
# Make sure your agentic_app/config/.env file contains:

# Required: OpenAI API Key for embeddings
AGENTIC_APP_OPENAI_API_KEY=your_openai_api_key_here

# Qdrant Configuration (default values shown)
AGENTIC_APP_QDRANT_URL=http://127.0.0.1:6333
# AGENTIC_APP_QDRANT_API_KEY=your_qdrant_api_key_here  # Optional, only for cloud instances

# Embedding model (default: text-embedding-3-small)
AGENTIC_APP_EMBEDDING_MODEL=text-embedding-3-small
